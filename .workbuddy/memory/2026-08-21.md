# 2026-08-21

## 项目优化：P0 / P1 / P2 落地

### P0 — RagService ThreadLocal 泄漏（已完成，编译通过）
- `retrievalTrace` / `retrievalScope` 两个 ThreadLocal 在 Web 线程池复用下跨请求泄漏（内存 + ownerUserId 越权读取隐患）。
- 对 `chatInternal`、`debugRetrieval`、`retrieveForAgent` 三个读写/设置路径加 `try { ... } finally { retrievalTrace.remove(); retrievalScope.remove(); }` 清理。

### P1 — DocumentIngestionService 并发锁（已完成，编译通过）
- `synchronized` 方法级锁改为 `ReentrantLock ingestionLock`，仅保护全局共享状态变更（documents / vectorStore / documentIndexStore）。
- 解析/OCR/落盘等慢速 IO 移出锁外，仅"共享状态变更"进临界区。

### P2 — 上帝类拆分（部分完成，编译通过）
- 新增 `rag/IngestionPathResolver.java`：从 DocumentIngestionService 抽取 16 个纯函数（路径解析/安全校验/哈希/类型判断），行为零变化，调用改为 `ingestionPathResolver.xxx()`。注意 `entry.ownerUserId()` 是 DocumentIndexEntry 方法，曾险些被全局替换误伤，已修正。
- 新增 `learning/LearningRecordFormatter.java`：从 LearningRecordService 抽取 9 个纯 Markdown 格式化/答案清洗方法 + `UNRELIABLE_ANSWER_MARKERS` 常量（未抽取与 WORKSPACE_MARKER/RECORD_ENTRY 纠缠的 questionFrom/answerFrom 等）。调用改为 `recordFormatter.xxx()`。
- RagService 答案护栏集群（614–757 行）**已安全抽取**为 `rag/RagAnswerGuardrail.java`：8 个纯函数（isUsableModelFallbackAnswer / containsInvalidCodeIdentifier / parseCodeBlocks / containsHanOutsideSqlText / asksForSql / asksForAllPostgresTables / isUnsupportedControlCharacter / isLikelyGibberishToken）+ 6 个静态正则（仅本集群使用，已随迁）+ 2 个内部 record（CodeBlock / ParsedCode）。调用改为 `answerGuardrail.isUsableModelFallbackAnswer(...)`。逻辑零变化，`mvn -o -q compile` 通过。注意保留 `EXPLICIT_TECHNICAL_ACRONYM` / `GENERIC_TECHNICAL_TERMS` 在 RagService（被 matchesExplicitTechnicalTerm 使用）。
- RagService（2073 行）4 路"编排级"拆分（QueryRewriter/RetrievalService/ChatOrchestrator）**仍不做**：核心 RAG 编排逻辑 + 无集成测试环境，盲拆风险高，留待测试环境执行。

### 环境约束（重要）
- 本机仅 Java17 + 本地 Maven 3.6.3（离线 `-o` 可用），无 Postgres / Chroma。但**测试全是 mock 单元测试（0 个 @SpringBootTest/Testcontainers），本机可直接 `mvn -o test` 跑全套 306 测试**；重构后务必跑 `mvn -o clean package`（含 test）验证，不能只靠 compile。
- **可直连服务器**：本机有部署私钥 `~/.ssh/shihai_deploy_key`，`ssh -i ~/.ssh/shihai_deploy_key -p 22 deploy@175.178.229.209` 可连（VM-0-7-opencloudos）。deploy 用户 sudoers 仅免密授权 `/usr/local/sbin/deploy-shihai` 一个命令；journalctl 仅见有限日志、`/opt/shihai/logs` 为 root 权限读不了。部署脚本 `[1/5]` 是 `mvn clean package`（不带 -DskipTests），test 失败会直接中止部署、远端不改动。
- 无 `timeout` 命令（macOS），长编译直接跑。

## 部署失败排查 + P0/P1 回归修复（下午）
- 现象：用户执行 `./deploy/deploy-shihai --yes` 报错。连服务器确认 `shihai.service` active running（since 10:20）、公网 health HTTP 200，跑的是旧版 `401c05b9`（重构前祖先）。说明部署在**本地 test 阶段就挂了**，从未上传，远端 rollback 保留旧版。
- 根因：`mvn clean package` 含 test 失败 2 例（之前只跑 `package -DskipTests` 漏检）：
  1. `DocumentIngestionServiceSyncTest`：P1 锁改造时 `indexWorkspaceUpload` 4 参版漏写 `documents.addAll(ingested.documents())`，索引文档未进内存列表。已补回（removeIf 之后、锁内）。
  2. `RagServiceHybridRetrievalTest`：P0 给 `debugRetrieval` 加 ThreadLocal finally 清理后，`expandAdjacent`（读 retrievalScope）拿不到 scope，相邻页扩展失败。改为优先用 scope、为空时从候选文档推导 owner/workspace。
- 修复提交 `d69888f`，`mvn -o clean package` 306 测试全过（BUILD SUCCESS）。
- 教训：P1 锁改造移动 add 逻辑时必须保留全部共享状态写入；P0 ThreadLocal 清理不能破坏依赖该 ThreadLocal 的独立方法（expandAdjacent 应自包含作用域）。

