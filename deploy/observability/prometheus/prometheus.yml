global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: shihai
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["${SHIHAI_METRICS_TARGET}"]
        labels:
          service: shihai
